[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn app:app --host 0.0.0.0 --port ${PORT:-8080}"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
